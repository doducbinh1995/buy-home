import React, { useMemo } from "react";

const PanoramaView = (props) => {
  return <div className="w-100 h-100 position-relative" id="panorama-container"></div>;
};

export default React.memo(PanoramaView);
